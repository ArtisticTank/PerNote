class User():
    def __init__(self, userId, userName, password, mailId, firstName, lastName):
        self.userId = userId
        self.userName = userName
        self.firstName = firstName
        self.lastName = lastName
        self.mailId = mailId
        self.password = password
